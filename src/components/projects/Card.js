import React from "react";
import styled from 'styled-components';

const CardWrapper = styled.div`
`;

function Card(props) {
    return (
        <CardWrapper>
            {}
        </CardWrapper>
    );
}

export default Card;
